{
  "name": "testing",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "cross-env NODE_ENV=test jest --verbose --forceExit",
    "test:watch": "cross-env NODE_ENV=test jest --watch --forceExit",
    "seed": "node seed.js",
    "migrate": "node -e \"require('dotenv').config(); const {pool}=require('./src/db'); const fs=require('fs'); pool.query(fs.readFileSync('production_upgrade.sql','utf8')).then(()=>{console.log('Migration complete');pool.end()}).catch(e=>{console.error(e);pool.end()})\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.13.5",
    "bcryptjs": "^3.0.3",
    "cloudinary": "^2.9.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "joi": "^18.0.2",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.1",
    "multer": "^2.0.2",
    "nodemailer": "^8.0.1",
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^30.2.0",
    "supertest": "^7.2.2"
  }
}
